# Tenant scripts

Canonical location for tenant JavaScript modules.

Current legacy scripts remain at tenant root for compatibility. They will be migrated here only after each dependency is separated and tested.

- `widget.js` is the current legacy AI widget entry.
- Future modules should be organized by responsibility: navigation, products, booking, CRM, knowledge, analytics, and integration.
