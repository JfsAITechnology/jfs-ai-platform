(() => { const C=window.JFS_CATALOG; const KEY='jfs-firman'; let state=JSON.parse(localStorage.getItem(KEY)||'{}'); state.schedule=state.schedule||{Online:'Jadwal Online belum diisi admin',Reguler:'29 Agustus 2026 · 09.00 WIB · Surabaya',Privat:'Atur jadwal dengan admin',Platinum:'Atur jadwal dengan admin'}; state.leads=state.leads||[]; const $=id=>document.getElementById(id); const money=n=>'Rp '+Number(n).toLocaleString('id-ID'); function save(){localStorage.setItem(KEY,JSON.stringify(state))} window.go=(id,btn)=>{document.querySelectorAll('.page').forEach(x=>x.classList.remove('on')); const p=$(id); if(p)p.classList.add('on'); document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('on')); if(btn)btn.classList.add('on'); if(id==='products')renderProducts(); if(id==='book')renderBooking(); if(id==='leads'||id==='crm')renderLeads(); if(id==='knowledge')renderKnowledge(); window.scrollTo(0,0)}; function renderProducts(){$('prodGrid').innerHTML=C.products.map((p,i)=>`<div class="card product"><img src="${p.image}" alt="${p.name}"><h3>${p.name}</h3><div class="price">${money(p.price)}</div><button class="btn" onclick="detail(${i})">Lihat Detail</button></div>`).join('')} window.detail=i=>{const p=C.products[i]; $('mt').textContent=p.name;$('mi').src=p.image;$('md').textContent=p.description;$('mp').textContent=money(p.price);$('modal').classList.add('show')}; window.closeM=()=>$('modal').classList.remove('show'); function renderBooking(){$('amcGrid').innerHTML=C.programs.slice(1).map(p=>`<div class="card amc"><img src="../assets/programs/amc-class.svg" alt="${p.name}"><h3>${p.name}</h3><p class="muted">Program ${p.type}</p><button class="btn" onclick="rprog.value='${p.name}'">Pilih</button></div>`).join('');$('scheduleCards').innerHTML=C.programs.map(p=>`<div class="card"><b>${p.name}</b><p class="muted">${state.schedule[p.type]}</p></div>`).join('')} function renderLeads(){const html=state.leads.length?state.leads.map(x=>`<div class="item"><b>${x.name}</b> · ${x.program} · Score ${x.score}</div>`).join(''):'<p class="muted">Belum ada lead.</p>'; $('leadList').innerHTML=html;$('crmList').innerHTML=html;$('nlead').textContent=state.leads.length;$('nhot').textContent=state.leads.filter(x=>x.score>=85).length;$('nbook').textContent=state.leads.length} window.register=()=>{const n=$('rn').value.trim();if(!n)return toast('Nama wajib diisi');const pr=$('rprog').value;state.leads.push({name:n,program:pr,score:pr.includes('Platinum')?95:pr.includes('Privat')?85:75});save();renderLeads();toast('Pendaftaran dicatat ke CRM')}; window.ask=()=>{const q=$('q').value.trim();if(!q)return;$('chat').innerHTML+=`<div class="msg me">${q}</div>`;const z=q.toLowerCase();let a='Saya belum menemukan informasi tersebut di Knowledge Database. Silakan hubungi Admin.';for(const p of C.products){if(z.includes(p.name.toLowerCase().split(' ').slice(1,3).join(' '))||z.includes('harga')&&z.includes(p.name.toLowerCase().split(' ')[1]))a=`${p.name}: ${p.description} Harga ${money(p.price)}.`}for(const p of C.programs){if(z.includes(p.type.toLowerCase())||z.includes(p.name.toLowerCase()))a=`${p.name}: ${state.schedule[p.type]}.`}$('chat').innerHTML+=`<div class="msg">${a}</div>`;$('q').value='';}; function renderKnowledge(){$('kview').innerHTML='<div class="item">Sumber saat ini: konfigurasi tenant + katalog modular. Data operasional akan dipindahkan ke Supabase pada fase berikutnya.</div>'} window.toast=m=>{const t=$('toast');t.textContent=m;t.style.display='block';setTimeout(()=>t.style.display='none',2200)}; window.wa=text=>window.open('https://wa.me/?text='+encodeURIComponent(text),'_blank'); renderProducts(); renderLeads(); })();