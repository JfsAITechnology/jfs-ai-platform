# Tenant pages

Canonical page entry points for Mas Firman Pratama.

During the compatibility phase, these entry points redirect to the existing live HTML files in the tenant root. This keeps existing public URLs stable while the implementation is migrated safely in later phases.

- `products.html` → existing `../products.html`
- `product-detail.html` → existing `../products-detail.html`
- `widget.html` → existing `../widget.html`
